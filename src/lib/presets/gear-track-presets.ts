// Re-export from season-config for legacy compatibility.
// All seasonal data lives in season-config.ts — this file only re-exports.
export { GEAR_TRACKS } from './season-config';
export type { GearTrack } from './season-config';
